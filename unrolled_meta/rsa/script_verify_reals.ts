import bundleExpired from "../../files/bundle.emre.expired.json" with { type: "json" };
import bundleValid from "../../files/bundle.emre.valid.json" with { type: "json" };

// binary payloads are base64
console.log(Object.keys(bundleExpired));
